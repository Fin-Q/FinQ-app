spring:
  config:
    activate:
      on-profile: prod

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:}

streak:
  statistics:
    enabled: ${STREAK_STATISTICS_ENABLED:true}
