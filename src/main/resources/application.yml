server:
  servlet:
    context-path: /api/v1

spring:
  application:
    name: FinQ

  profiles:
    default: local

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    baseline-on-migrate: false

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs
  model-converters:
    enum-as-ref: true
